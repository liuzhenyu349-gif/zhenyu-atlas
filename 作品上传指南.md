# 作品上传指南

这个网站是纯静态站点。作品的文字和图片会随代码一起提交到 GitHub，并由 GitHub Pages 自动发布。

## 添加一个作品

1. 如果已有真实封面，把图片放进 `public/projects/`，推荐使用 WebP 或 JPG，比例为 16:9。
2. 打开 `src/config.ts`。
3. 在 `projectsPageContent.projects` 数组中复制一个项目对象并修改：

```ts
{
  title: "作品名称",
  description: "一句话介绍作品的目标、过程或结果。",
  year: "2026",
  status: "Coming Soon",
}
```

没有完成的项目请保留 `status: "Coming Soon"`，不要填写 `image` 和 `url`。项目完成后再加入：

```ts
image: "/projects/your-project.webp",
url: "https://真实的项目详情链接",
```

## 修改个人资料

- 姓名、头像、邮箱、社交链接：`src/config.ts` 顶部。
- 首页、关于页、经历、作品和文章标题：`src/config.ts`。
- 头像文件：替换 `public/logo.webp`。
- 关于页图片：替换 `public/demo-1.jpg`，或在配置中换成新路径。

## 本地预览

```bash
npm install
npm run dev
```

浏览器访问 `http://localhost:4321`。

## 发布到 GitHub Pages

1. 在 GitHub 创建一个空仓库。
2. 把本目录推送到仓库的 `main` 分支。
3. 打开仓库的 `Settings → Pages`。
4. 在 `Build and deployment` 的 `Source` 中选择 `GitHub Actions`。
5. 再次推送代码，等待 Actions 完成。

仓库名可以是任意名称；部署配置会自动处理项目站点的子路径。如果仓库名是 `你的用户名.github.io`，网址就是 `https://你的用户名.github.io/`。
